# Dagster workspace configuration
# Defines code locations that contain Dagster definitions

load_from:
  - grpc_server:
      host: dagster-user-code
      port: 4000
      location_name: banking_pipeline
